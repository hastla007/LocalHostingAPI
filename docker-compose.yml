version: "3.9"

services:
  localhostingapi:
    build: .
    container_name: localhostingapi
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/app/uploads
      - data:/app/app/data
    environment:
      - PORT=8000

volumes:
  uploads:
  data:
