version: "3.9"

services:
  localhostingapi:
    build: .
    container_name: localhostingapi
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/uploads
      - data:/app/data
    environment:
      - PORT=8000

volumes:
  uploads:
  data:
